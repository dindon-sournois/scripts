#!/bin/bash

if [[ "$(hostname -s)" == "msi" ]] && [ "$XDG_VTNR" -eq 1]; then
    synergy&
    lxpolkit&
fi

if [ "$XDG_VTNR" -eq 1 ]; then
	firefox&
	thunderbird&
	emacs --daemon
fi

if [ "$XDG_VTNR" -eq 2 ]; then
	steam-native
fi

# i3-subscribe
# xcmenu -daemon
nm-applet&
dunst&
i3-focus-last&
